'use client';
import Link from 'next/link';import {Layers3} from 'lucide-react';import{appConfig}from'@/config/env';import{useCategories}from'../../hooks/useCategories';import{EmptyState}from'@/components/shared/EmptyState';
export function CategoriesScreen(){const{categories,loading}=useCategories(appConfig.defaultShopId);return <main className="p-4"><h1 className="mb-4 text-2xl font-extrabold">Categories</h1>{loading?<p>Loading categories…</p>:categories.length===0?<EmptyState title="No categories yet" message="Please check back soon." icon={<Layers3 className="h-12 w-12"/>}/>:<div className="grid grid-cols-2 gap-3 md:grid-cols-4">{categories.map(c=><Link key={c.id} href={`/category/${c.id}`} className="flex min-h-28 items-center justify-center rounded-lg border bg-background p-4 text-center text-lg font-bold">{c.name}</Link>)}</div>}</main>}
